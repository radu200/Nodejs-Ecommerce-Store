
      {{#if message.success}} 
    {{#each message.success}}
    <div class="alert alert-danger">
        {{msg}}
    </div>
    {{/each}}
 {{/if}}
{{#each results}}
<div class="row">
    <div class="container">
      <div class="col-md-12">
 <img src='/images/{{this.image}}' class="responsive-img"  alt="" id="avatarImage" >
  </div>
    </div>
  </div>
{{/each}}
 
 <form  method="POST" action="/users/avatar" enctype="multipart/form-data">
    <div class="form-group">
        <input type='file' name='avatar'>
        <input  class="btn btn-primary" type="submit" name="submit">
  </form>